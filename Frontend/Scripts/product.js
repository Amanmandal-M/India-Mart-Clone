const baseUrl = `http://localhost:5094`
const defaultUrl = `${baseUrl}/products`
const datasUrl = `${defaultUrl}/BricksData`             //GET
const dataByIdUrl = `${defaultUrl}/Bricks/${id}`        //GET BY ID
